[build]
  command   = "pnpm run build"      # or "npm run build" if you prefer npm
  publish   = ".netlify/output"      # where the plugin will emit your static assets
  functions = ".netlify/functions"   # where the plugin will emit your SSR/Edge functions

[[plugins]]
  package = "@netlify/plugin-nextjs"
